# PostgreSQL config
POSTGRES_USER=postgresadmin
POSTGRES_PASSWORD=postgresadmin
POSTGRES_DB=medical_rag_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Elasticsearch
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_PASSWORD=elasticsearchadmin
ELASTICSEARCH_URL=http://${ELASTICSEARCH_HOST}:${ELASTICSEARCH_PORT}

# Qdrant Vector Database
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_URL=http://${QDRANT_HOST}:${QDRANT_PORT}

# redis config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redisadmin
REDIS_DB=0
CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}
CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}


# HuggingFace Hub
HF_TOKEN=
HF_USERNAME=quannguyen204

# OpenAI API
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# Cohere API 
COHERE_API_KEY=

# Tavily API 
TAVILY_API_KEY=

# Backend API (for embedding/rerank/guardrails models)
BACKEND_API_HOST=localhost
BACKEND_API_PORT=8000

# Qwen3 Models GPU Service (NEW: separate GPU service for optimal performance)
QWEN3_MODELS_URL=http://localhost:8002
QWEN3_MODELS_ENABLED=true  # Set to false to use local CPU models

